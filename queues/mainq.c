#include "q.h"
// #include "q.c"

int main() {
    queue *q;
    create_queue(&q);
    enqueue(q, 5);
    enqueue(q, 1);
    enqueue(q, 2);
    printf()
    dequeue(dequeue(q))
    dequeue(dequeue(q))
    destroy_queue()
}